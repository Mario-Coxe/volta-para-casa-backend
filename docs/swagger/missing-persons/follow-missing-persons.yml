openapi: 3.0.0
info:
  title: Missing Persons
  version: 1.0.0
  description: Missing Persons
paths:
  /v1/api/missing-persons/follow/{id}:
    post:
      summary: Observar caso de uma pessoa desaparecida
      security:
        - bearerAuth: []
      tags:
        - Missing Persons
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        200:
          description: Caso em observação
        404:
          description: Pessoa desaparecida não encontrada
