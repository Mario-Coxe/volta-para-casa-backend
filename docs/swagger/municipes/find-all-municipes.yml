openapi: 3.0.0
info:
  title: Municipes
  version: 1.0.0
  description: Municipes
paths:
  /v1/api/municipes:
    get:
      summary: Retorna todas os municipios
      tags:
        - Municipes
      parameters:
        - in: query
          name: page
          schema:
            type: integer
          description: Número da página
        - in: query
          name: limit
          schema:
            type: integer
          description: Número de itens por página
      responses:
        200:
          description: Lista de todos os municipios

